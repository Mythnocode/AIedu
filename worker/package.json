{
  "name": "@exam-analyzer/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply exam-analyzer-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply exam-analyzer-db --remote"
  },
  "dependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.55.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1"
  }
}
